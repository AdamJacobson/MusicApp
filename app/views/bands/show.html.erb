<h1><%= link_to "All Bands", bands_url, method: :get %> / <%= @band.name %></h1>

<%= button_to "Create an Album", new_band_album_url(@band), method: :get %>
<%= button_to "Update Band", edit_band_url(@band), method: :get %>

<br>

<!-- Show all Albums -->

<%= render @band.albums %>

<br>
