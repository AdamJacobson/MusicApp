<h1><%= render 'bands/link', band: @album.band %>
  / <%= @album.name %></h1>

Recording Type: <%= @album.live? ? "Live" : "Studio" %>

<br><br>

<%= button_to "Add a Track", new_album_track_url(@album), method: :get %>

<br>

<!-- Show all tracks -->
<%= render @tracks %>
